import 'package:flutter/material.dart';

class Map extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text('地图'),),
      body: Center(
        child: Text('map.dart',style: TextStyle(fontSize: 18),),
      ),
    );
  }
}
