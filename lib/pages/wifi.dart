import 'package:flutter/material.dart';

class Wifi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement b
    return Scaffold(
      appBar: AppBar(title: Text('Wifi'),),
      body: Center(
        child: Text('wifi.dart',style: TextStyle(fontSize: 18),),
      ),
    );
  }
}
